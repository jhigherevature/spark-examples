package sparktools.controllers;

public class BatchFileController {
	public static void run() {
		/*
		 * Determine if batch files exist
		 * 		if not, run generation
		 * 		if so, ask for what to execute (for starters, only ask to write lab files)
		 */
	}
}
