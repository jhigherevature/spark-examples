package fixtures;

/*
 * The Interactable interface declares an abstract method
 * 'interact'. This will be used (in combination with the
 * instanceof operator) to determine objects we can interact
 * with in our rooms.
 */
public interface Interactable {
	public void interact();
}
